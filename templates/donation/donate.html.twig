{% extends 'base.html.twig' %}

{% block title %}Support: {{ caseSocial.title }}{% endblock %}

{% block body %}
    <div class="row justify-content-center">
        <div class="col-md-8">
            <div class="card mb-4">
                <div class="card-header bg-light">
                    <h3 class="mb-0">Support This Case</h3>
                </div>
                <div class="card-body">
                    <h5>{{ caseSocial.title }}</h5>
                    <p class="text-muted">{{ caseSocial.description|slice(0, 300) }}{% if caseSocial.description|length > 300 %}…{% endif %}</p>

                    <div class="alert alert-info">
                        <strong>💡 How it works:</strong> You declare your donation amount here, then you'll be redirected to Cha9a9a for secure payment.
                    </div>

                    {{ form_start(form, {'attr': {'novalidate': 'novalidate'}}) }}
                        <div class="mb-3">
                            {{ form_label(form.amount) }}
                            {{ form_widget(form.amount, {'attr': {'class': 'form-control form-control-lg', 'step': '0.01', 'min': '1'}}) }}
                            {{ form_errors(form.amount) }}
                            <small class="text-muted">Enter amount in MAD (Moroccan Dirham)</small>
                        </div>

                        <div class="mb-3">
                            {{ form_label(form.note) }}
                            {{ form_widget(form.note, {'attr': {'class': 'form-control', 'rows': '3', 'placeholder': 'Optional: Add a message of support (optional)'}}) }}
                            {{ form_errors(form.note) }}
                        </div>

                        <div class="d-grid gap-2 d-md-flex justify-content-md-between">
                            <a href="{{ path('app_case_social_show', {id: caseSocial.id}) }}" class="btn btn-outline-secondary">Back to Case</a>
                            <button class="btn btn-primary btn-lg" type="submit">💳 Continue to Payment</button>
                        </div>
                    {{ form_end(form) }}
                </div>
            </div>

            <!-- Info -->
            <div class="alert alert-light">
                <strong>📌 Next step:</strong> After confirming, you'll be taken to our secure payment partner Cha9a9a where you can complete the transaction. Your donation will be recorded as DECLARED.
            </div>
        </div>
    </div>
{% endblock %}
